<div class="row">
  <div class="small-6 small-offset-3 columns">
    <h1 class="header">Add Highlights to your trip</h1>

    <%= form_for [@trip, @highlight] do |f| %>

    <div class="field">
      <%= f.label :address %>
      <%= f.text_field :address, id: "address" %>
    </div>

    <div class="field">
      <%= f.label :latitude %>
      <%= f.number_field(:latitude) %>
    </div>

    <div class="field">
      <%= f.label :longitude %>
      <%= f.number_field(:longitude) %>
    </div>

    <div class="field">
      <%= f.label :location_name %>
      <%= f.text_field :location_name %>
    </div>

    <div class="field">
      <%= f.label :memory %>
      <%= f.text_area :memory %>
    </div>

    <div class="field">
      <%= f.label :highlight_photo, "Highlight Photo" %><br />
      <%= f.file_field :highlight_photo %>
    </div>

      <%= f.submit "Create Highlight" %>

    <% end %>
  </div>
</div>
